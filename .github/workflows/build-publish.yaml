name: Build and publish pypi package

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'

jobs:
  build_publish:
    name: Build and publish package
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: write
    environment:
      name: pypi
      url: https://pypi.org/p/parse-changelog

    steps:

    - name: Check out the repo 📘
      uses: actions/checkout@v3

    - name: Set up Python 3.11 🐍
      uses: actions/setup-python@v3
      with:
        python-version: "3.11"

    - name: Install tools 🛠️
      run: |-
        sudo apt-get install --yes make
        python -m pip install build setuptools twine wheel bump2version

    - name: Check unreleased changelog entries 📋
      if: startsWith(github.ref, 'refs/heads/main')
      run: |-
        unreleased_changes=$(bin/parse-changelog --show-unreleased)
        unreleased_entries=$(printf '%s\n' "${unreleased_changes}" | sed '/^[[:space:]]*###/d')
        if [[ -z "${unreleased_entries//[[:space:]]/}" ]]; then
          echo "No unreleased changelog updates found."
          exit 1
        fi

    - name: Get the next version 📈
      run: |-
        git fetch --tags --force --prune --prune-tags
        current_version=$(git tag --list 'v*' | tr -d 'v' | sort -Vur | head -n1)
        bump2version_output=$(bump2version --dry-run --list --current-version "${current_version}" patch)
        new_version=$(printf '%s\n' "${bump2version_output}" | awk -F= '$1 == "new_version" {print $2}')
        [[ "${new_version}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || exit 1
        printf 'NEW_VERSION=%s\n' "${new_version}" >> "${GITHUB_ENV}"

    - name: Update CHANGELOG with release 📋
      if: startsWith(github.ref, 'refs/heads/main')
      run: |-
        bin/parse-changelog --release "${NEW_VERSION}"

    - name: Build package 📦
      run: >-
        make package TAG="${NEW_VERSION}"

    - name: Check package metadata 🔎
      if: startsWith(github.ref, 'refs/heads/main')
      run: |-
        if compgen -G 'dist/*0.0.0*' > /dev/null; then
          echo "Refusing to publish package artifacts with fallback version 0.0.0"
          ls -la dist
          exit 1
        fi
        twine check dist/*

    - name: Publish package to PyPI 🎁
      if: startsWith(github.ref, 'refs/heads/main')
      # pypa/gh-action-pypi-publish v1.9.0
      uses: pypa/gh-action-pypi-publish@ec4db0b4ddc65acdf4bff5fa45ac92d78b56bdf0

    - name: Commit CHANGELOG and tag 💾
      if: startsWith(github.ref, 'refs/heads/main')
      run: |-
        if ! git diff -s --exit-code CHANGELOG.md; then
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add CHANGELOG.md
          git tag -a "v${NEW_VERSION}" -m "Version ${NEW_VERSION}"
          git commit -m "Update CHANGELOG for new release [ci skip]"
          git push origin HEAD:main --follow-tags
        fi
